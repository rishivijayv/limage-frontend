query UserUploadedImages($paginatedInput: PaginatedInput!){
  uploadedImages(paginatedInput:$paginatedInput)
  {
    hasMore
    entities {
      id
      location
      label
      createdAt
    }
  }
}